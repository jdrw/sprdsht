<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :gmail %><br />
    <%= f.text_field :gmail %>
  </div>
  <div class="field">
    <%= f.label :gpassword %><br />
    <%= f.text_field :gpassword %>
  </div>
  <div class="field">
    <%= f.label :drive_id %><br />
    <%= f.text_field :drive_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
